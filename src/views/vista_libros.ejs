<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vista Admin Libros</title>
    <style>
        *{
            margin: 0;
            padding: 0;
        }
        #principal{
            display: flex;
            height: 100vh;
            width: 100%;
            flex-direction: column;
        }
        #cont_menu{
            display: flex;
            justify-content: space-around;
            height: 10%;
            width: 100%;
            align-items: center;
        }

        #footer{
            display: flex;
            flex-direction: column;
            width: 100%;
            height: 10%;
            align-items: center;
        }
    </style>
</head>
<body>
    <section id="principal">
        <div id="cont_menu">
            <div>
                <p>Home</p>
            </div>
            <div>
                <p>Profile</p>
                <a href="/login">Logout</a>
            </div>
        </div>
        <div>
            <% for(let i = 0; i < libros.length; i++){ %>
                <div id="vista_libros">
                    <img src="<%= libros[i].portada %>" alt="">
                    <p><%= libros[i].nombre_libro %></p>
                    <p><%= libros[i].descripcion %></p>
                </div>
            <% } %>
        </div>
        <footer id="footer">
            <p>footer</p>
        </footer>
    </section>
</body>
</html>