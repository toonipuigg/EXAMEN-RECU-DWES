<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome</title>
    <style>
        *{
            margin: 0;
            padding: 0;
        }
        #principal{
            display: flex;
            flex-direction: column;
            height: 100vh;
            width: 100%;
        }
        #cont_menu{
            display: flex;
            justify-content: space-around;
            height: 10%;
            width: 100%;
            align-items: center;
            flex-direction: row;
        }
        #nombre_usuario{
            display: flex;
            align-items: center;
            justify-content: center;
            width: 100%;
            height: 80%;
            flex-direction: column;
            font-size: 30px;
        }
        #footer{
            display: flex;
            flex-direction: column;
            width: 100%;
            height: 10%;
            align-items: center;
        }
        #boton{
            border-radius: 10px;
            text-decoration: none;
            color: black;
            background-color: grey;
            font-size: 30px;
        }
    </style>
</head>
<body>
    <section id="principal">
        <div id="cont_menu">
            <div>
                <p>Home</p>
            </div>
            <div>
                <p>Profile</p>
                <a href="/login">Logout</a>
            </div>
        </div>
        <div id="nombre_usuario">
            <% for(let i = 0; i < usuarios.length ;i++){ %>
                <p>Bienvenido: <%=usuarios[i].nombre_usuario%></p>
            <% } %>
            <a id="boton" href="/catalogo">Empecemos</a>
        </div>
        <footer id="footer">
            <p>footer</p>
        </footer>
    </section>
</body>
</html>