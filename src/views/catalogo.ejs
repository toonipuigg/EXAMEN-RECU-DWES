<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Catalogo</title>
    <style>
        *{
            margin: 0;
            padding: 0;
        }
        #principal{
            display: flex;
            height: 100vh;
            width: 100%;
            flex-direction: column;
        }
        #cont_menu{
            display: flex;
            justify-content: space-around;
            height: 10%;
            width: 100%;
            align-items: center;
        }
        #contenedor{
            display: flex;
            flex-direction: column;
            align-items: center;
            width: 100%;
            height: 80%;
        }
        #vista_libros{
            display: flex;
            flex-direction: column;
            justify-content: space-around;
            height: 50%;
            width: 30%;
            align-items: center;
        }
        img{
            height: 100%;
            width: 100%;
        }
        #footer{
            display: flex;
            flex-direction: column;
            width: 100%;
            height: 10%;
            align-items: center;
        }
    </style>
</head>
<body>
    <section id="principal">
        <div id="cont_menu">
            <div>
                <p>Home</p>
            </div>
            <div>
                <p>Profile</p>
                <a href="/login">Logout</a>
            </div>
        </div>
        <div id="contenedor">
            <a href="/add_libros">Añadir</a>
            <% for(let i = 0; i < libros.length; i++){%>
                <div id="vista_libros">
                    <img src="<%= libros[i].portada %>" alt="">
                    <p><%= libros[i].nombre_libro %></p>
                    <p><%= libros[i].descripcion %></p>
                    <form action="/delete_libro" method="post">
                        <input type="hidden" name="id_libro" value="<%= libros[i].id %>">
                        <input type="submit" value="Eliminar">
                    </form>
                    <a href="/update_libro">Editar</a>
                </div>
            <% } %>
        </div>
        <footer id="footer">
            <p>footer</p>
        </footer>
    </section>
</body>
</html>